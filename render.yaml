services:
  - type: web
    name: tasks-backend
    runtime: java
    buildCommand: cd backend && mvn clean package -DskipTests
    startCommand: java -jar backend/target/tasks-backend-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: 8080
